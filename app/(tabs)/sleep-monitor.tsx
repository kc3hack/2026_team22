/**
 * Sleep Monitor Tab
 * ルーティング定義のみ - 実装は src/features/sleep-monitor から import
 */
import { SleepMonitorScreen } from '@features/sleep-monitor/SleepMonitorScreen';

export default function SleepMonitorTab() {
  return <SleepMonitorScreen />;
}
